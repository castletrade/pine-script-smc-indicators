name: Pine Script Quality Audit

on: push

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Validation
      run: |
        ls README.md LICENSE SECURITY.md
        echo "Validating Pine Script logic patterns..."
        # Check for //@version=5
        grep -r "//@version=" . 
